// Supabase client — install @supabase/supabase-js when ready for Module 3
// For now, progress is stored in localStorage only.

export const supabase: any = null;

export function isSupabaseConfigured(): boolean {
  return false;
}
